export enum Pages {
  GAME = 'game',
  ABOUT = 'about',
  MENU = 'menu',
  OPTIONS = 'options',
  GAMEPLAY = 'gameplay',
  VISUAL = 'visual',
  OPTIONS_GAMEPLAY = OPTIONS + '/' + GAMEPLAY,
  OPTIONS_VISUAL = OPTIONS + '/' + VISUAL
}
